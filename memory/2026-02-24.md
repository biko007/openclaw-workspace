# 2026-02-24

- User required strict SharePoint filing behavior for Schornsteinfeger invoices:
  - Final target folder is root-level `Scans` (same level as `2-bikolino GmbH`), not nested under `2-bikolino GmbH`.
  - Miscreated folders were cleaned up: removed `2-bikolino GmbH/Scans` and `2-bikolino GmbH/Dokumente/Scans` (and empty `Dokumente` under `2-bikolino GmbH`).
- User required exact filenames and extension handling:
  - `2026-02-19_MG24 Rechnung Schornsteinfeger.pdf`
  - `2026-02-20_S28 Rechnung Schornsteinfeger.pdf`
  - User emphasized: no additions/omissions in names; `.pdf` suffix mandatory.
- User requested sharing with Doris (doris.hilsenbeck@bikolino.com): completed by sending SharePoint links via email.
- Trip/document linking:
  - Linked SharePoint file `DB_Ticket_333224892960.pdf` from root `Scans` to trip `HAN` as label `DB Ticket`.
- Dashboard assets:
  - Added uploaded image as property image for L19 (stored as `property-l19.jpg` for assets dashboard rendering).
- Briefing location preference/logic:
  - User stated briefing must always consider current location at every call.
  - Executive-agent briefing logic updated to resolve location dynamically each time:
    1) current calendar event location (now ±30 min),
    2) active trip destination geocoding,
    3) fallback to saved location.
